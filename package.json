{
  "scripts": {
    "sass:dev": "sass --watch --embed-source-map src/scss:public/stylesheets",
    "sass:prod": "sass --no-source-map --style compressed src/scss:public/stylesheets"
  },
  "devDependencies": {
    "sass": "^1.69.7"
  }
}
